import { TokenCollateralEntity } from './token-collateral.entity';

describe('TokenCollateralEntity', () => {
  it('should be defined', () => {
    expect(new TokenCollateralEntity()).toBeDefined();
  });
});
