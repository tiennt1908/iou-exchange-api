import { DappContractTypeEntity } from './dapp-contract-type.entity';

describe('DappContractTypeEntity', () => {
  it('should be defined', () => {
    expect(new DappContractTypeEntity()).toBeDefined();
  });
});
