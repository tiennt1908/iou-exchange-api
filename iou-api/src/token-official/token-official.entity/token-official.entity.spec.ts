import { TokenOfficialEntity } from './token-official.entity';

describe('TokenOfficialEntity', () => {
  it('should be defined', () => {
    expect(new TokenOfficialEntity()).toBeDefined();
  });
});
